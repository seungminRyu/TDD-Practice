import { useEffect, useState } from "react";

function Products(params) {
    const [productImgs, setProductImgs] = useState([]);

    useEffect(() => {}, [productImgs]);

    const loadProductImgs = async () => {};
}

export default Products;
